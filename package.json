{
    "scripts": {
        "build": "tsc",
        "dev": "tsnd --respawn --notify=false -T src",
        "start": "node dist",
        "db:migrate": "prisma migrate dev --preview-feature && prisma generate"
    },
    "dependencies": {},
    "devDependencies": {
        "@types/node": "^16.3.3",
        "ts-node-dev": "^1.1.8",
        "typescript": "^4.3.5"
    }
}
